# Roadmap

Phase-level plan for {{REPO_DISPLAY_NAME}}. Current phase at the top.
Update at the end of each phase (move finished items into a `Closed`
section, add the new phase at the top). Don't grow the "next" phase
list while current-phase items are still open.

> AGENT: the project's `ROADMAP.md` is what a new session reads FIRST
> after `AGENTS.md` (per the Session Start read order). Keep it
> honest — don't leave closed items in the active phase, don't pad
> the next phase with aspirational items that have no clear owner.
> The shape that tends to work:

```
## Phase N — <name> (current)

- [ ] <item 1 — concrete, with what "done" looks like>
- [ ] <item 2>
- [ ] <item 3>

Status line: <one-line summary of where the phase is right now>.
Top risks / known unknowns: <the things that could blow up the
estimate or block forward motion — be honest>.

## Phase N+1 — <name> (next, unstarted)

- <high-level items, leave room for the phase-N completion to
  reshape the next-phase scope>

## Closed phases

### Phase N-1 — <name> — closed YYYY-MM-DD

- [x] <item that closed>
- [x] <item that closed>
```

Replace this stub with the actual roadmap once you know what the
project's first phase should be.
