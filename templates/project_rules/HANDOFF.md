# Handoff

`Last updated`: `YYYY-MM-DD HH:MM UTC` (bootstrap stub — replace on first
session end)

> AGENT: this file is the session-to-session state. Update it on
> every session end (and any time the state materially changes mid-
> session). Keep `Current state` short — 1 paragraph. `Top 3 next
> actions` is a numbered list, not prose. `Blockers` is empty if
> there are none.

## Current state

Bootstrap stub. Replace on first session end with the actual current
state of the project (one short paragraph).

## Top 3 next actions

1. (empty)
2. (empty)
3. (empty)

## Blockers

None.

## Notes for the next session

> AGENT: anything that doesn't fit into `Current state` /
> `Top 3 next actions` but a future session needs to know to avoid
> re-discovering it the hard way. Add entries here when you learn
> something non-obvious; prune when it becomes stale.
